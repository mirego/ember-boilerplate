import Ember from 'ember';

export default Ember.Controller.extend({
  // NOTE: The line below is optional. You can use this line if you need translations into your controller.
  // i18n: Ember.inject.service()
});
